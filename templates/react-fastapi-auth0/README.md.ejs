# 🧞 <%= projectName %>

    Generated by **Full Stack Genie** - Your full-stack project scaffolding genie!

    ## 🚀 Quick Start

    ### Prerequisites
    - Node.js 18+ and npm
    - Python 3.9+
    - Git

    ### Installation

    1. **Install Frontend Dependencies**
    ```bash
    cd frontend
    npm install
    ```

    2. **Install Backend Dependencies**
    ```bash
    cd backend
    python -m venv venv
    source venv/bin/activate # On Windows: venv\Scripts\activate
    pip install -r requirements.txt
    ```

    3. **Configure Environment Variables**

    Copy the example env files and fill in your values:
    ```bash
    cp frontend/.env.example frontend/.env
    cp backend/.env.example backend/.env
    ```

    <% if (authDomain) { %>
        **⚠️ Important:** Update your Auth0 credentials in both `.env` files!
        <% } %>

            ### Running the Application

            **Option 1: Run Both Servers Separately**

            Terminal 1 - Backend:
            ```bash
            cd backend
            python main.py
            ```

            Terminal 2 - Frontend:
            ```bash
            cd frontend
            npm run dev
            ```

            **Option 2: Use VS Code Tasks** (Coming Soon)

            Access your app:
            - Frontend: http://localhost:<%= frontendPort %>
                - Backend API: http://localhost:<%= backendPort %>
                    - API Docs: http://localhost:<%= backendPort %>/docs

                        ## 📁 Project Structure

                        ```
                        <%= projectName %>/
                            ├── frontend/ # React + Vite
                            │ ├── src/
                            │ │ ├── App.jsx # Main application component
                            │ │ ├── main.jsx # Entry point
                            │ │ └── *.css # Styling
                            │ ├── package.json
                            │ └── vite.config.js
                            ├── backend/ # FastAPI
                            │ ├── main.py # API server
                            │ ├── config.py # Configuration
                            │ └── requirements.txt
                            └── README.md # You are here!
                            ```

                            ## 🎯 What's Included

                            - ✅ React 18 with Vite (Fast HMR)
                            - ✅ FastAPI with Uvicorn
                            - ✅ CORS configured for local development
                            - ✅ Environment variable management
                            <% if (authDomain) { %>
                                - ✅ Auth0 integration scaffolding
                                <% } %>
                                    <% if (databaseUrl) { %>
                                        - ✅ Database connection setup
                                        <% } %>
                                            - ✅ Git initialized with proper .gitignore
                                            - ✅ Pre-configured proxy for API calls

                                            ## 🔌 API Endpoints

                                            | Method | Endpoint | Description |
                                            |--------|----------|-------------|
                                            | GET | `/` | Welcome message |
                                            | GET | `/api/hello` | Test connection |
                                            | GET | `/api/health` | Health check |
                                            | GET | `/docs` | Swagger API documentation |

                                            ## 🛠️ Development Tips

                                            ### Frontend
                                            - Edit `src/App.jsx` to start building your UI
                                            - Vite HMR provides instant feedback
                                            - Use `axios` for API calls (already configured)

                                            ### Backend
                                            - Add new routes in `main.py`
                                            - FastAPI auto-generates docs at `/docs`
                                            - Use `reload=True` for auto-restart on changes

                                            ### Common Issues

                                            **Port already in use?**
                                            ```bash
                                            # Kill process on port <%= backendPort %>
                                                lsof -ti:<%= backendPort %> | xargs kill -9
                                                    ```

                                                    **CORS errors?**
                                                    - Check `backend/main.py` CORS configuration
                                                    - Ensure frontend URL matches `allow_origins`

                                                    ## 📦 Building for Production

                                                    ### Frontend
                                                    ```bash
                                                    cd frontend
                                                    npm run build
                                                    # Output in frontend/dist
                                                    ```

                                                    ### Backend
                                                    ```bash
                                                    cd backend
                                                    pip freeze > requirements.txt
                                                    # Deploy with gunicorn or similar
                                                    ```

                                                    ## 🎨 Customization

                                                    - **Change Ports:** Update `vite.config.js` and `backend/.env`
                                                    - **Add Pages:** Create components in `frontend/src/`
                                                    - **Add API Routes:** Extend `backend/main.py`
                                                    <% if (databaseUrl) { %>
                                                        - **Database Models:** Create models in `backend/models/`
                                                        <% } %>

                                                            ## 🐛 Debugging

                                                            1. **Check Backend Logs:**
                                                            - Look for errors in the terminal running `main.py`
                                                            - Visit http://localhost:<%= backendPort %>/docs to test
                                                                endpoints

                                                                2. **Check Frontend Console:**
                                                                - Open browser DevTools (F12)
                                                                - Look for network errors or CORS issues

                                                                3. **Verify Environment Variables:**
                                                                ```bash
                                                                cat backend/.env
                                                                cat frontend/.env
                                                                ```

                                                                ## 📚 Tech Stack

                                                                ### Frontend
                                                                - **React 18:** UI library
                                                                - **Vite:** Build tool (faster than Webpack)
                                                                - **Axios:** HTTP client

                                                                ### Backend
                                                                - **FastAPI:** Modern Python web framework
                                                                - **Uvicorn:** ASGI server
                                                                - **Pydantic:** Data validation
                                                                <% if (databaseUrl) { %>
                                                                    <% if (databaseUrl.includes('postgresql')) { %>
                                                                        - **PostgreSQL:** Database
                                                                        - **SQLAlchemy:** ORM
                                                                        <% } %>
                                                                            <% if (databaseUrl.includes('mongodb')) { %>
                                                                                - **MongoDB:** Database
                                                                                - **Motor:** Async MongoDB driver
                                                                                <% } %>
                                                                                    <% } %>

                                                                                        ## 🤝 Contributing

                                                                                        This project was generated by
                                                                                        Full Stack Genie. Feel free to:
                                                                                        - Add features
                                                                                        - Fix bugs
                                                                                        - Improve documentation

                                                                                        ## 📄 License

                                                                                        MIT License - Feel free to use
                                                                                        this for your hackathon project!

                                                                                        ---

                                                                                        **Generated by Full Stack Genie
                                                                                        🧞** -
                                                                                        Skip the boilerplate, start
                                                                                        coding in 60 seconds!

                                                                                        Need help? Check out:
                                                                                        - [FastAPI
                                                                                        Docs](https://fastapi.tiangolo.com/)
                                                                                        - [React
                                                                                        Docs](https://react.dev/)
                                                                                        - [Vite
                                                                                        Docs](https://vitejs.dev/)