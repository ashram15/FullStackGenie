<template>
    <div id="app">
        <header>
            <h1>🧞 <%= projectName %>
            </h1>
            <p>Built with Vue 3 + Vite | Generated by Full Stack Genie</p>
        </header>

        <main>
            <section class="card">
                <h2>Welcome to your hackathon project!</h2>
                <p>Your full-stack application is ready to go.</p>

                <button @click="testBackend" class="test-button">
                    Test Backend Connection
                </button>

                <div v-if="apiResponse" class="response">
                    <h3>✅ Backend Response:</h3>
                    <pre>{{ JSON.stringify(apiResponse, null, 2) }}</pre>
                </div>

                <div v-if="error" class="error">
                    <h3>❌ Error:</h3>
                    <p>{{ error }}</p>
                </div>
            </section>

            <section class="info">
                <h2>🚀 Quick Start</h2>
                <div class="steps">
                    <div class="step">
                        <h3>1. Frontend (Vue 3)</h3>
                        <code>cd frontend && npm install && npm run dev</code>
                        <p>Runs on port <%= frontendPort %>
                        </p>
                    </div>
                    <div class="step">
                        <h3>2. Backend (FastAPI)</h3>
                        <code>cd backend && pip install -r requirements.txt && python main.py</code>
                        <p>Runs on port <%= backendPort %>
                        </p>
                    </div>
                </div>
            </section>
        </main>
    </div>
</template>

<script>
    import { ref } from 'vue'
    import axios from 'axios'

    export default {
        name: 'App',
        setup() {
            const apiResponse = ref(null)
            const error = ref(null)

            const testBackend = async () => {
                try {
                    error.value = null
                    const response = await axios.get('/api/hello')
                    apiResponse.value = response.data
                } catch (err) {
                    error.value = err.message
                    apiResponse.value = null
                }
            }

            return {
                apiResponse,
                error,
                testBackend,
                JSON
            }
        }
    }
</script>

<style scoped>
    #app {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
    }

    header {
        text-align: center;
        margin-bottom: 3rem;
        padding: 2rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border-radius: 12px;
    }

    h1 {
        font-size: 2.5rem;
        margin: 0;
    }

    .card {
        padding: 2rem;
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem;
    }

    .test-button {
        background: #667eea;
        color: white;
        border: none;
        padding: 12px 24px;
        border-radius: 8px;
        cursor: pointer;
        font-size: 1rem;
        margin: 1rem 0;
    }

    .test-button:hover {
        background: #5568d3;
    }

    .response,
    .error {
        margin-top: 1rem;
        padding: 1rem;
        border-radius: 8px;
    }

    .response {
        background: #d4edda;
        color: #155724;
    }

    .error {
        background: #f8d7da;
        color: #721c24;
    }

    .info {
        padding: 2rem;
        background: #f8f9fa;
        border-radius: 12px;
    }

    .steps {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-top: 1.5rem;
    }

    .step {
        padding: 1.5rem;
        background: white;
        border-radius: 8px;
        border-left: 4px solid #667eea;
    }

    code {
        display: block;
        padding: 0.75rem;
        background: #282c34;
        color: #61dafb;
        border-radius: 4px;
        margin: 0.5rem 0;
        overflow-x: auto;
    }
</style>